apiVersion: batch/v1
kind: CronJob
metadata:
  name: cleanup
  namespace: demo
spec:
  schedule: "0 2 * * *"
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: OnFailure
          containers:
            - name: cleanup
              image: app:latest
              command:
                - ./cleanup
