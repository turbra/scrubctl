apiVersion: apps/v1
kind: Deployment
metadata:
  name: web
  namespace: demo
  annotations:
    example.com/keep: 'yes'
  finalizers:
    - example.com/custom
spec:
  template:
    spec:
      containers:
        - name: web
          image: nginx:latest
