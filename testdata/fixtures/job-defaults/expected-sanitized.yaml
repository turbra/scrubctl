apiVersion: batch/v1
kind: Job
metadata:
  name: migrate
  namespace: demo
spec:
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: migrate
          image: app:latest
          command:
            - ./migrate
